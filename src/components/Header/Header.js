import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <div className="App">
        MY HEADER
      </div>
    );
  }
}

export default Header;

// Stateful component
// use when access to the state/functionality is needed
